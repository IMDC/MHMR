import { useEffect } from "react";
import RNFS from 'react-native-fs';

//converts all the files in the selectedVideos set to selected
const convertToSelected = (selectedVideos: Set<string>) => {
    //useEffect to convert the files to selected
    
}

export default convertToSelected;